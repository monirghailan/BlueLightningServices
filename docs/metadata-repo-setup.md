# Metadata mirror — not used

We **do not** maintain a separate Salesforce metadata repo (`bls-salesforce`) or run metadata sync.

**Portal Assistant source of truth:** human markdown you write in [`bls-org-docs/{client-slug}`](https://github.com/bls-org-docs), indexed via `github_repo_url` in Supabase.

See [`assistant-repo-setup.md`](./assistant-repo-setup.md) for the full runbook.
